apiVersion: 1

providers:
  - name: 'Lite Bank Dashboards'
    orgId: 1
    folder: 'Lite Bank'
    folderUid: 'litebank'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
